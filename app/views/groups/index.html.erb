<h1>Groups</h1>

<ul>
  <% @groups.each do |group| %>
    <li>
      <%= link_to group.name, group %>
    </li>
  <% end %>
</ul>

<%= link_to "New Group", new_group_path %>

<h2>Add a Group:</h2>
<%= render 'form', group: @groups.build %>
