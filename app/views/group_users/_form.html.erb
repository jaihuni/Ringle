<%= form_with model: [ @group, @group.group_users.build ] do |form| %>
  <p>
    <%= form.label :user_id %><br>
    <%= form.text_field :user_id %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>
